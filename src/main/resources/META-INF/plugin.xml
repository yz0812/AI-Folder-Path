<idea-plugin>
    <id>com.github.aifolderpath</id>
    <name>AI Folder Path</name>
    <vendor>AIFolderPath</vendor>
    <description><![CDATA[
    <p>Copy AI-ready paths, symbol anchors, context blocks, usages, project trees, and Git commit context from JetBrains IDEs.</p>
    <p><b>中文</b></p>
    <ul>
      <li>支持 Copy AI / Path / Anchor / Context / Usages / Tree / Git Info 多种 AI 友好输出</li>
      <li>Alt+P 按当前默认动作复制；在 Git Log 中复制修订号，增强动作不可用时自动回退为路径复制</li>
      <li>支持文件、目录、多选路径、目录树、类、方法、代码选区、调用点列表与 Git Log 提交信息</li>
      <li>支持 Maven / Gradle 多模块路径解析，适合 Claude、Cursor、Copilot Chat 等 AI 编程工具</li>
      <li>内置快捷键设置页，可配置当前可用动作，并自动处理只读 Keymap 副本</li>
    </ul>
    <p><b>English</b></p>
    <ul>
      <li>Supports Copy AI / Path / Anchor / Context / Usages / Tree / Git Info for AI-ready code references</li>
      <li>Alt+P uses the current default action, copies revisions in Git Log, and falls back to path copy when enhanced actions are unavailable</li>
      <li>Handles files, directories, multi-selection paths, directory trees, classes, methods, selections, call-site lists, and Git Log commit info</li>
      <li>Resolves Maven / Gradle multi-module paths for Claude, Cursor, Copilot Chat, and other AI coding tools</li>
      <li>Includes a built-in shortcut settings page for currently available actions, with read-only keymap duplication support</li>
    </ul>
    ]]></description>

    <resource-bundle>messages.AIFolderPathBundle</resource-bundle>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>
    <depends optional="true" config-file="aifolderpath-with-java.xml">com.intellij.modules.java</depends>
    <depends optional="true" config-file="aifolderpath-with-vcs.xml">com.intellij.modules.vcs</depends>

    <extensions defaultExtensionNs="com.intellij">
        <notificationGroup id="AIFolderPath.Notification"
                           displayType="BALLOON"
                           key="notification.group.aifolderpath"/>
        <applicationService serviceImplementation="com.github.aifolderpath.settings.NotificationSettings"/>
        <projectService serviceImplementation="com.github.aifolderpath.settings.ProjectPathSettings"/>
        <projectConfigurable
                parentId="tools"
                id="com.github.aifolderpath.settings"
                instance="com.github.aifolderpath.settings.UnifiedSettingsConfigurable"
                displayName="AI Folder Path"
                nonDefaultProject="true"/>
    </extensions>

    <actions>
        <action id="AIFolderPath.CopyOptionsAction"
                class="com.github.aifolderpath.CopyAIOptionsAction"
                text="Copy AI"
                description="Copy AI-friendly reference using the current Alt+P action">
            <keyboard-shortcut keymap="$default" first-keystroke="alt P"/>
        </action>
        <group id="AIFolderPath.ProjectViewCopyGroup"
               text="Copy AI"
               popup="true">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
            <action id="AIFolderPath.CopyAction"
                    class="com.github.aifolderpath.CopyAIPathAction"
                    text="Copy AI Path"
                    description="Copy file path in AI-friendly format"/>
            <action id="AIFolderPath.CopyTreeAction"
                    class="com.github.aifolderpath.CopyAITreeAction"
                    text="Copy AI Tree"
                    description="Copy selection as an AI-friendly tree"/>
        </group>
    </actions>
</idea-plugin>
